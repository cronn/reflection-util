rootProject.name = "reflection-util"

try {
    if (Runtime.version().feature() >= 17) {
        include "java-17-tests"
    }
} catch (MissingMethodException ignore) {
    // Do not include java-17-tests on even older JVMs
}
